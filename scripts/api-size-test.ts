const login = await fetch("http://localhost:4200/api/auth/login", {   method: "POST",   headers: { "Content-Type": "application/json" },   body: JSON.stringify({ email: "admin@crm.local", password: "admin123" }), }); const cookie = login.headers.get("set-cookie") || ""; const res = await fetch("http://localhost:4200/api/conversations", { headers: { cookie } }); const text = await res.text(); const data = JSON.parse(text); console.log("status:", res.status, "bytes:", text.length, "convs:", data.conversations?.length); process.exit(0);