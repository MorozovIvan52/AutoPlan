const login = await fetch("http://localhost:4200/api/auth/login", {   method: "POST",   headers: { "Content-Type": "application/json" },   body: JSON.stringify({ email: "admin@crm.local", password: "admin123" }), }); const cookie = login.headers.get("set-cookie") || "";  for (const id of [346, 345, 67]) {   const res = await fetch(`http://localhost:4200/api/conversations/${id}/messages`, { headers: { cookie } });   const data = await res.json() as any;   console.log(`conv#${id}:`, res.status, "msgs:", data.messages?.length, data.error || data.messages?.[0]?.text?.slice(0, 40)); }  process.exit(0);