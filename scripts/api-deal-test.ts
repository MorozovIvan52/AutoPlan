const login = await fetch("http://localhost:4200/api/auth/login", {   method: "POST",   headers: { "Content-Type": "application/json" },   body: JSON.stringify({ email: "admin@crm.local", password: "admin123" }), }); const cookie = login.headers.get("set-cookie") || "";  const res = await fetch("http://localhost:4200/api/deals", {   method: "POST",   headers: { "Content-Type": "application/json", cookie },   body: JSON.stringify({ clientId: 1, title: "╨в╨╡╤Б╤В ╨╖╨░╨║╨░╨╖ API", orderType: "parts", status: "new", amount: 1000 }), }); const text = await res.text(); console.log("status:", res.status, "bytes:", text.length, "body:", text.slice(0, 200)); process.exit(0);