const login = await fetch("http://localhost:4200/api/auth/login", {   method: "POST",   headers: { "Content-Type": "application/json" },   body: JSON.stringify({ email: "admin@crm.local", password: "admin123" }), }); const cookie = login.headers.get("set-cookie") || ""; console.log("login", login.status);  const due = new Date(); due.setMinutes(due.getMinutes() + 2);  const res = await fetch("http://localhost:4200/api/tasks", {   method: "POST",   headers: { "Content-Type": "application/json", cookie },   body: JSON.stringify({     title: "╨в╨╡╤Б╤В ╨╖╨░╨┤╨░╤З╨░ ╨╕╨╖ API",     priority: "high",     assignedTo: 1,     clientId: 1,     dueAt: due.toISOString(),   }), }); const text = await res.text(); console.log("POST task", res.status, text.slice(0, 300));  const list = await fetch("http://localhost:4200/api/tasks", { headers: { cookie } }); console.log("GET tasks", list.status, (await list.text()).slice(0, 200));