const login = await fetch("http://localhost:4200/api/auth/login", {   method: "POST",   headers: { "Content-Type": "application/json" },   body: JSON.stringify({ email: "admin@crm.local", password: "admin123" }), }); const cookie = login.headers.get("set-cookie") || ""; console.log("login", login.status);  const list = await fetch("http://localhost:4200/api/deals", { headers: { cookie } }); const deals = await list.json(); const id = deals.deals?.[0]?.id; console.log("first deal id", id);  if (id) {   const one = await fetch(`http://localhost:4200/api/deals/${id}`, { headers: { cookie } });   const text = await one.text();   console.log("GET deal", one.status, text.slice(0, 400)); }