# SUMMARY — полный аудит CRM (2026-07-23)

**Цель:** проверить прод после restore модулей; прогнать QA / Docs / Migration / full-audit.  
**Code Fixer:** план по P1 ниже — **правки кода не вносились** (нужно ваше «делай»).

---

## Вердикт

| Слой | Результат |
|------|-----------|
| Live smoke API + бандл (restore) | **PASS** |
| UI все страницы NAV (Playwright local → prod) | **PASS** (test 01) |
| API GET smoke (+ cdek/zzap/buyouts/payroll/ai) | **PASS** (test 02) |
| ЗН close-with-payment | **FAIL P1** — нет остатка у race-запчасти |
| Чат demo messages | **FAIL P1** — stale/битый conversationId |
| Склад / sales / tenant isolation / UI deals·warehouse·sales | **PASS** (05–10) |
| `pilot:verify` на VPS | **PASS** |
| Unit tests на VPS | **PASS** |
| `tsc --noEmit` на VPS | **FAIL** (старые typing issues) |
| Playwright на самом VPS | **FAIL infra** — нет `libatk-1.0.so.0` |
| Migration | **не мигрировать** — SQLite, Postgres не готов |
| Docs | drift vs ADMIN-CRM / старый «14 страниц» — чеклист обновлён |

**Итог для пилота:** основные экраны и API после restore **живы**. Блокеры — сценарий закрытия ЗН на нулевом остатке и demo-чат; typecheck/VPS chromium — техдолг, не «пропавшее меню».

---

## Агенты

| Агент | ID / артефакт | Кратко |
|-------|----------------|--------|
| **QA** | [QA](53a5f5cd-6c91-4425-95f3-76d7a9c074c4) → `docs/agents/reports/_agent_53a5f5cd.md` | Coverage gap в e2e до sync; settings OK; money = scaffold |
| **Docs** | [Docs](1ff63263-f80b-4814-be82-fa6fa28e0530) → `_agent_1ff63263.md` | ADMIN-CRM устарел (procurement/gantt/…); checklist дополнен |
| **Migration** | [Migration](099741b5-3706-426b-9411-508f1fa3125a) → `_agent_099741b5.md` | Остаёмся на SQLite до «готов к Postgres» |
| **Full-audit script** | VPS `/tmp/crm-full-audit-report.md` → `docs/agents/reports/2026-07-23_FULL-AUDIT.md` | 2 PASS / 2 FAIL (tsc + PW libs) |
| **Code Fixer** | не запускался на правки | ждёт «делай» по плану ниже |

Сделано в ходе аудита (не Code Fixer product-fix):
- расширен `e2e/helpers/pilot-auth.ts` (`UI_ROUTES` + `API_GET_SMOKE`);
- обновлён `docs/agents/CRM-AUDIT-CHECKLIST.md`.

---

## Live smoke (prod `sto-1`)

Все **200**: health, clients, cdek/shipments, buyouts, zzap/status+lists, payroll/roles+my, ai/status, crm/settings, sales.

В бандле есть: `/delivery`, `/zzap`, `/buyouts`, `/payroll`, `/my-salary`, `/assistant`, `/zn`, `/money`, «Расчёт ЗП», «Моя зарплата», «Доставка».  
Settings: **без** `nav-mobile`; вкладки security/alerts/general/sales на месте.

---

## Playwright (локально → https://crmavito.online)

| # | Тест | Результат |
|---|------|-----------|
| 01 | UI все страницы | PASS |
| 02 | API GET smoke | PASS |
| 03 | ЗН → labor → part → close-with-payment | **FAIL** `409 INSUFFICIENT_STOCK` (PILOT-1-RACE qty 0) |
| 04 | чат messages | **FAIL** (messages endpoint / stale id) |
| 05 | склад + race ≥ 0 | PASS |
| 06 | sales | PASS |
| 07 | изоляция tenant | PASS |
| 08–10 | UI deals / warehouse / sales | PASS |

---

## P0 / P1 (для Code Fixer — ждать «делай»)

### P1 — ЗН close: недостаточно остатка
- **Факт:** `POST /api/sto/deals/:id/close-with-payment` → `409 INSUFFICIENT_STOCK` на `PILOT-1-RACE` (нужно 1, есть 0).
- **Почему:** e2e берёт `parts[0]`; race-запчасть уже выжжена демо-прогонами.
- **План фикса:** в audit выбирать part с `qty > 0` (или `pilot:seed` пополнить); опционально не падать на 0-qty в seed.

### P1 — чат demo
- **Факт:** test 04 — `messages` не ok.
- **План:** сверить `pilot-demo-manifest.json` conversationId со sto-1; fallback на первую живую conversation уже есть — добить статус/права.

### P2 — typecheck
- `ClientPanel` / analytics / clients / dashboard / settings — `Property X does not exist on '{}'`.
- `api/lib/avito-poll.ts` missing modules; `server.ts` WsClient; avito-cpa-monitor comparison.
- **План:** точечная типизация query results; не трогать `.env`.

### P2 — VPS Playwright
- Нет `libatk-1.0.so.0` у chromium headless.
- **План:** `apt install` deps Playwright **или** гонять e2e с рабочей станции (как сейчас).

### Docs (не код)
- Пометить в ADMIN-CRM: procurement/gantt/team-activity/billing/book = API-only / нет UI.
- Добавить **Деньги** в карту модулей.

---

## Migration

**Не мигрировать.** SQLite на VPS. Postgres URL в локальном `.env` на хост `db` — недоступен с этой машины; RLS не активен. Ждать явной команды владельца.

---

## Следующий шаг

Напишите **«делай»** (и что чинить: ЗН seed / чат / typecheck / VPS chromium / docs ADMIN) — тогда Code Fixer внесёт минимальный diff и повторит затронутые тесты.
