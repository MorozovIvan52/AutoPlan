# CRM: контроль заказ-нарядов

Source: `docs/agents/workflows/sto-work-orders-review.yaml`
Generated: 2026-07-22T09-37-41
Exit: 0

Не могу выдать отчёт: файлы AGENTS.md, api/routes/deals.ts, src/pages/zn.tsx, api/lib/deal-totals.ts, api/lib/stock-reserve.ts, api/services/service-notify.ts не приложены к сообщению. Без исходников и данных CRM любой список ZN, клиентов и остатков будет выдумкой — а это прямо запрещено в моих правилах.

## Что прислать, чтобы я отдал готовый отчёт

Прикрепите текстом (или ссылками) содержимое файлов и/или дампы таблиц:

**1. Канбан ЗН (из api/routes/deals.ts + src/pages/zn.tsx)**
Поля: `id`, `client_name`, `client_phone`, `stage`, `stage_entered_at`, `parts[]`, `works[]`, `reserved_qty`, `notify_last_sent_at`.

**2. Логика расчёта итогов (api/lib/deal-totals.ts)** — чтобы понимать, что считается «пустой позицией».

**3. Остатки и резервы (api/lib/stock-reserve.ts)** — таблица `stock` (sku, qty, reserved) и связь с позициями ЗН.

**4. Шаблон уведомления (api/services/service-notify.ts)** — чтобы текст SMS попал в ваш формат, а не в мой.

**5. AGENTS.md** — принятые в команде пороги времени на стадии и интервал уведомлений.

## Что я сделаю сразу, как получу данные

Прогоню четыре проверки и выдам строго четыре блока:

| Проверка | Источник | Что ищем |
|---|---|---|
| ЗН без позиций | `deals.parts.length + works.length == 0` при `stage != 'closed'` | id, клиент, стадия |
| Долго на стадии | `now() - stage_entered_at > AGENTS.md.threshold` | id, клиент, дней, стадия |
| Резерв > остатка | `stock.reserved > stock.qty` или `deal.reserved > stock.qty` | sku, ЗН, клиент, дефицит |
| Клиенту не писали | `now() - notify_last_sent_at > notify.interval` | id, клиент, телефон, дней |

## Формат вывода (как договорено)

```
## ЗН требуют внимания (id, клиент, проблема)
- ЗН-XXXX, Иванов И.И., «Нет позиций, висит 4 дня на 'Диагностика'»

## Запчасти заказать / зарезервировать
- SKU-1234 (масло 5W30), дефицит 3 шт., ЗН-1002, 1005, 1007 — заказать 6 шт.

## SMS/уведомления клиенту (текст-черновик)
- ЗН-XXXX, +7…: «Здравствуйте, {Имя}. По вашему {Марка} {Гос.номер}: диагностика завершена, согласуйте стоимость работ…»

## Задачи для приёмщика и мастера
- Приёмщику: перезвонить клиенту ЗН-XXXX до 18:00
- Мастеру: закрыть работы по ЗН-XXXX, перевести в «Готов к выдаче»
```

Пришлите файлы или выгрузку — закрою отчёт одним проходом без догадок.

