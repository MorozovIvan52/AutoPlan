export function telHref(phone: string): string {   const digits = phone.replace(/\D/g, "");   if (!digits) return "";   const normalized = digits.length === 11 && digits.startsWith("8")     ? `7${digits.slice(1)}`     : digits.length === 10       ? `7${digits}`       : digits;   return `tel:+${normalized}`; }  export function formatPhoneDisplay(phone: string): string {   return phone.trim(); }