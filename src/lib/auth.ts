import { createContext, useContext } from "react";  export type User = {   id: number;   name: string;   email: string;   role: "admin" | "operator";   theme?: string;   avatarUrl?: string; };  export const AuthContext = createContext<{   user: User | null;   setUser: (u: User | null) => void; }>({ user: null, setUser: () => {} });  export const useAuth = () => useContext(AuthContext);