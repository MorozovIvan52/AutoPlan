import { Switch, Route, useLocation, Redirect } from "wouter"; import { useEffect, useState } from "react"; import { Providers } from "./components/provider"; import { useAuth } from "./lib/auth"; import { readResponseBody } from "./lib/api-errors"; import LoginPage from "./pages/login"; import InboxPage from "./pages/inbox"; import ClientsPage from "./pages/clients"; import AnalyticsPage from "./pages/analytics"; import SettingsPage from "./pages/settings"; import DealsPage from "./pages/deals"; import TasksPage from "./pages/tasks"; import WarehousePage from "./pages/warehouse"; import DashboardPage from "./pages/dashboard"; import RepairsPage from "./pages/repairs"; import CalendarPage from "./pages/calendar"; import CallsPage from "./pages/calls"; import MarketingPage from "./pages/marketing"; import TeamChatPage from "./pages/team-chat";
import SalesPage from "./pages/sales";   function AuthGate({ children }: { children: React.ReactNode }) {   const { user, setUser } = useAuth();   const [loading, setLoading] = useState(true);   const [, setLocation] = useLocation();    const applyUser = (data: { user?: { theme?: string } } | null) => {     if (data?.user) {       setUser(data.user as any);       if (data.user.theme) {         document.documentElement.setAttribute("data-theme", data.user.theme === "dark-navy" ? "" : data.user.theme);       }     } else {       setUser(null);     }   };    const revalidateAuth = () =>     fetch("/api/auth/me", { credentials: "include" })       .then(async (res) => {         if (res.ok) applyUser(await readResponseBody(res) as any);         else applyUser(null);       })       .catch(() => applyUser(null));    useEffect(() => {     revalidateAuth().finally(() => setLoading(false));     const onFocus = () => { revalidateAuth(); };     window.addEventListener("focus", onFocus);     return () => window.removeEventListener("focus", onFocus);   }, []);    if (loading) {     return (       <div style={{ height: "100vh", background: "var(--bg)", display: "flex", alignItems: "center", justifyContent: "center" }}>         <div style={{ textAlign: "center", color: "var(--text-muted)" }}>           <div style={{ fontSize: 40, marginBottom: 12 }}>ЁЯТм</div>           <p style={{ fontFamily: "Poppins", fontSize: 16 }}>AutoService CRM</p>         </div>       </div>     );   }    return <>{children}</>; }  function ProtectedRoute({ component: Component }: { component: React.ComponentType }) {   const { user } = useAuth();   if (!user) return <Redirect to="/login" />;   return <Component />; }  function AppRoutes() {   return (     <AuthGate>       <Switch>         <Route path="/login" component={LoginPage} />         <Route path="/dashboard" component={() => <ProtectedRoute component={DashboardPage} />} />         <Route path="/" component={() => <ProtectedRoute component={InboxPage} />} />         <Route path="/team" component={() => <ProtectedRoute component={TeamChatPage} />} />         <Route path="/clients" component={() => <ProtectedRoute component={ClientsPage} />} />         <Route path="/deals" component={() => <ProtectedRoute component={DealsPage} />} />         <Route path="/warehouse" component={() => <ProtectedRoute component={WarehousePage} />} />         <Route path="/sales" component={() => <ProtectedRoute component={SalesPage} />} />         <Route path="/tasks" component={() => <ProtectedRoute component={TasksPage} />} />         <Route path="/calendar" component={() => <ProtectedRoute component={CalendarPage} />} />         <Route path="/calls" component={() => <ProtectedRoute component={CallsPage} />} />         <Route path="/repairs" component={() => <ProtectedRoute component={RepairsPage} />} />         <Route path="/service" component={() => <ProtectedRoute component={RepairsPage} />} />         <Route path="/marketing" component={() => <ProtectedRoute component={MarketingPage} />} />         <Route path="/analytics" component={() => <ProtectedRoute component={AnalyticsPage} />} />         <Route path="/settings" component={() => <ProtectedRoute component={SettingsPage} />} />         <Route component={() => <Redirect to="/" />} />       </Switch>     </AuthGate>   ); }  export default function App() {   return (     <Providers>       <AppRoutes />     </Providers>   ); }