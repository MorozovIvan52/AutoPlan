import type { CSSProperties } from "react";  export function Skeleton({ width, height, style }: { width?: string | number; height?: string | number; style?: CSSProperties }) {   return (     <div       className="skeleton"       style={{ width: width || "100%", height: height || 16, ...style }}     />   ); }  export function ConvSkeleton() {   return (     <div className="conv-item" style={{ pointerEvents: "none" }}>       <Skeleton width={44} height={44} style={{ borderRadius: "50%", flexShrink: 0 }} />       <div style={{ flex: 1 }}>         <Skeleton height={14} style={{ marginBottom: 6, width: "60%" }} />         <Skeleton height={12} style={{ width: "85%" }} />       </div>     </div>   ); }