import { getInitials, getAvatarColor } from "../lib/utils";  export function Avatar({ name, size = 36, url }: { name: string; size?: number; url?: string | null }) {   if (url) {     return <img src={url} alt={name} style={{ width: size, height: size, borderRadius: "50%", objectFit: "cover", flexShrink: 0 }} />;   }   return (     <div style={{       width: size, height: size,       borderRadius: "50%",       background: getAvatarColor(name),       display: "flex", alignItems: "center", justifyContent: "center",       color: "#fff",       fontSize: size * 0.35,       fontWeight: 700,       fontFamily: "Poppins",       flexShrink: 0,     }}>       {getInitials(name)}     </div>   ); }