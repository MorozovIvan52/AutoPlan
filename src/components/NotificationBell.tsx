import { useState } from "react";  import { useLocation } from "wouter";  import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";  import { apiFetch, apiFetchVoid } from "../lib/fetch-api";  import { useAuth } from "../lib/auth";  import { formatTime } from "../lib/utils";    export function NotificationBell() {    const { user } = useAuth();    const [open, setOpen] = useState(false);    const [, setLocation] = useLocation();    const qc = useQueryClient();      const { data } = useQuery({      queryKey: ["notifications", user?.id],      queryFn: () => apiFetch<{ notifications: any[]; unread: number }>("/api/notifications"),      enabled: !!user,      refetchInterval: 15000,    });      const readMutation = useMutation({      mutationFn: (id: number) => apiFetchVoid(`/api/notifications/${id}/read`, { method: "PATCH" }),      onSuccess: () => qc.invalidateQueries({ queryKey: ["notifications"] }),    });      const readAllMutation = useMutation({      mutationFn: () => apiFetchVoid("/api/notifications/read-all", { method: "PATCH" }),      onSuccess: () => qc.invalidateQueries({ queryKey: ["notifications"] }),    });      const unread = data?.unread || 0;    const items = data?.notifications || [];      return (      <div style={{ position: "relative", padding: "0 8px" }}>        <button          type="button"          className="nav-item"          onClick={() => setOpen(!open)}          title="╨г╨▓╨╡╨┤╨╛╨╝╨╗╨╡╨╜╨╕╤П"          style={{ position: "relative", fontSize: 18 }}        >          <span className="nav-item__icon">ЁЯФФ</span>          <span className="nav-label">╨г╨▓╨╡╨┤╨╛╨╝╨╗╨╡╨╜╨╕╤П</span>          {unread > 0 && <span className="nav-badge">{unread > 9 ? "9+" : unread}</span>}        </button>        {open && (          <>            <div style={{ position: "fixed", inset: 0, zIndex: 40 }} onClick={() => setOpen(false)} />            <div style={{              position: "absolute", left: 0, bottom: "100%", marginBottom: 8, width: 320, maxHeight: 400,              background: "var(--card)", border: "1px solid var(--border)", borderRadius: 12,              boxShadow: "0 8px 32px #0006", zIndex: 50, overflow: "hidden",            }}>              <div style={{ padding: "12px 16px", borderBottom: "1px solid var(--border)", display: "flex", justifyContent: "space-between", alignItems: "center" }}>                <span style={{ fontWeight: 600, fontSize: 13 }}>╨г╨▓╨╡╨┤╨╛╨╝╨╗╨╡╨╜╨╕╤П</span>                {unread > 0 && (                  <button type="button" onClick={() => readAllMutation.mutate()} style={{ fontSize: 11, color: "var(--accent)", background: "none", border: "none", cursor: "pointer" }}>                    ╨Я╤А╨╛╤З╨╕╤В╨░╤В╤М ╨▓╤Б╨╡                  </button>                )}              </div>              <div style={{ maxHeight: 340, overflowY: "auto" }}>                {items.length === 0 && (                  <p style={{ padding: 20, color: "var(--text-muted)", fontSize: 13, textAlign: "center" }}>╨Э╨╡╤В ╤Г╨▓╨╡╨┤╨╛╨╝╨╗╨╡╨╜╨╕╨╣</p>                )}                {items.map((n: any) => (                  <div                    key={n.id}                    onClick={() => {                      if (!n.readAt) readMutation.mutate(n.id);                      if (n.link) { setLocation(n.link); setOpen(false); }                    }}                    style={{                      padding: "10px 16px", borderBottom: "1px solid var(--border)", cursor: "pointer",                      background: n.readAt ? "transparent" : "var(--accent)11",                    }}                  >                    <p style={{ fontWeight: 600, fontSize: 12 }}>{n.title}</p>                    {n.text && <p style={{ fontSize: 11, color: "var(--text-muted)", marginTop: 2 }}>{n.text}</p>}                    <p style={{ fontSize: 10, color: "var(--text-muted)", marginTop: 4 }}>{formatTime(n.createdAt)}</p>                  </div>                ))}              </div>            </div>          </>        )}      </div>    );  } 