type Props = {   label: string;   value: string | null;   editing: boolean;   editVal: string;   onEditStart: () => void;   onEditChange: (val: string) => void;   onEditSave: () => void;   onEditCancel: () => void; };  export function EditableFieldRow({   label, value, editing, editVal,   onEditStart, onEditChange, onEditSave, onEditCancel, }: Props) {   return (     <div style={{ marginBottom: 10 }}>       <label style={{ fontSize: 11, color: "var(--text-muted)", display: "block", marginBottom: 2 }}>{label}</label>       {editing ? (         <div style={{ display: "flex", gap: 6 }}>           <input             className="crm-input"             value={editVal}             onChange={(e) => onEditChange(e.target.value)}             style={{ height: 32, fontSize: 13 }}             autoFocus           />           <button type="button" className="crm-btn" style={{ height: 32, padding: "0 10px", fontSize: 12 }} onClick={onEditSave}>тЬУ</button>           <button type="button" className="crm-btn crm-btn-ghost" style={{ height: 32, padding: "0 8px", fontSize: 12 }} onClick={onEditCancel}>тЬХ</button>         </div>       ) : (         <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>           <span style={{ fontSize: 13, color: value ? "var(--text)" : "var(--text-muted)" }}>{value || "тАФ"}</span>           <button             type="button"             onClick={onEditStart}             style={{ background: "none", border: "none", color: "var(--text-muted)", cursor: "pointer", fontSize: 12, padding: "2px 6px" }}           >тЬПя╕П</button>         </div>       )}     </div>   ); }