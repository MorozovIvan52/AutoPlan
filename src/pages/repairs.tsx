import { useState } from "react";  import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";  import { apiFetch, apiFetchVoid } from "../lib/fetch-api";  import { AppShell } from "../components/AppShell";  import { useToast } from "../lib/toast";  import { combineDateTime, todayDateInput, defaultReminderTime } from "../lib/datetime";    const STATUS_LABELS: Record<string, string> = {    scheduled: "Записан",    confirmed: "Подтверждён",    in_progress: "В ремонте",    done: "Готов",    cancelled: "Отменён",  };    function formatDateTime(iso: string) {    const d = new Date(iso);    const date = d.toLocaleDateString("ru-RU", { day: "2-digit", month: "2-digit", year: "2-digit" });    const time = d.toLocaleTimeString("ru-RU", { hour: "2-digit", minute: "2-digit" });    return `${date} ${time}`;  }    function carModel(a: any): string {    const v = a.vehicle;    if (!v) return "—";    return [v.make, v.model].filter(Boolean).join(" ") || "—";  }    export default function RepairsPage() {    const qc = useQueryClient();    const { toast } = useToast();    const [notifyWa, setNotifyWa] = useState(false);      const [clientId, setClientId] = useState("");    const [plate, setPlate] = useState("");    const [model, setModel] = useState("");    const [mileage, setMileage] = useState("");    const [phone, setPhone] = useState("");    const [title, setTitle] = useState("");    const [apptDate, setApptDate] = useState(todayDateInput());    const [apptTime, setApptTime] = useState(defaultReminderTime());    const [notes, setNotes] = useState("");      const { data: apptData, isLoading } = useQuery({      queryKey: ["repair-appointments"],      queryFn: () => apiFetch<{ appointments: any[] }>("/api/service/appointments"),    });      const { data: clientsData } = useQuery({      queryKey: ["clients-list-repairs"],      queryFn: () => apiFetch<{ clients: { id: number; name: string; phone?: string }[] }>("/api/clients"),    });      const createMutation = useMutation({      mutationFn: () => {        const scheduledAt = combineDateTime(apptDate, apptTime);        if (!scheduledAt) throw new Error("Укажите дату и время");        if (!title.trim()) throw new Error("Укажите работы");        return apiFetch("/api/service/appointments", {          method: "POST",          headers: { "Content-Type": "application/json" },          body: JSON.stringify({            clientId: clientId ? Number(clientId) : null,            plate: plate.trim() || null,            model: model.trim() || null,            mileage: mileage.trim() || null,            phone: phone.trim() || null,            title: title.trim(),            scheduledAt,            notes: notes.trim() || null,            notifyWhatsApp: notifyWa,          }),        });      },      onSuccess: (res: any) => {        qc.invalidateQueries({ queryKey: ["repair-appointments"] });        setPlate("");        setModel("");        setMileage("");        setPhone("");        setTitle("");        setNotes("");        if (res?.notifyResult?.ok) toast("Запись добавлена · WhatsApp отправлен", "success");        else if (res?.notifyResult?.error) toast(`Запись добавлена · ${res.notifyResult.error}`, "info");        else toast("Запись добавлена", "success");      },      onError: (e: Error) => toast(e.message, "error"),    });      const updateMutation = useMutation({      mutationFn: ({ id, status }: { id: number; status: string }) =>        apiFetch(`/api/service/appointments/${id}`, {          method: "PATCH",          headers: { "Content-Type": "application/json" },          body: JSON.stringify({ status, notifyWhatsApp: notifyWa }),        }),      onSuccess: () => {        qc.invalidateQueries({ queryKey: ["repair-appointments"] });        toast("Статус обновлён", "success");      },      onError: (e: Error) => toast(e.message, "error"),    });      const cancelMutation = useMutation({      mutationFn: (id: number) => apiFetchVoid(`/api/service/appointments/${id}`, { method: "DELETE" }),      onSuccess: () => {        qc.invalidateQueries({ queryKey: ["repair-appointments"] });        toast("Запись отменена", "info");      },      onError: (e: Error) => toast(e.message, "error"),    });      const appointments = apptData?.appointments || [];    const canSubmit = title.trim() && apptDate && apptTime;      return (      <AppShell hideTopBar>        <div className="page-header">          <h1 className="page-title">Запись на ремонт</h1>          <label style={{ display: "flex", alignItems: "center", gap: 6, fontSize: 12, cursor: "pointer" }}>            <input type="checkbox" checked={notifyWa} onChange={(e) => setNotifyWa(e.target.checked)} />            WhatsApp при записи (если указан телефон)          </label>        </div>          <div className="page-body">          <div className="crm-card" style={{ padding: 16, marginBottom: 16, borderRadius: 12 }}>            <p style={{ fontWeight: 600, fontSize: 13, marginBottom: 10 }}>+ Новая запись</p>            <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(120px, 1fr))", gap: 8 }}>              <input className="crm-input" type="date" value={apptDate} onChange={(e) => setApptDate(e.target.value)} title="Дата" />              <input className="crm-input" type="time" value={apptTime} onChange={(e) => setApptTime(e.target.value)} title="Время" />              <input className="crm-input" placeholder="Госномер" value={plate} onChange={(e) => setPlate(e.target.value)} style={{ textTransform: "uppercase" }} />              <input className="crm-input" placeholder="Модель" value={model} onChange={(e) => setModel(e.target.value)} />              <input className="crm-input" placeholder="Работы *" value={title} onChange={(e) => setTitle(e.target.value)} style={{ gridColumn: "span 2" }} />              <input className="crm-input" placeholder="Пробег" value={mileage} onChange={(e) => setMileage(e.target.value)} />              <input className="crm-input" placeholder="Телефон" value={phone} onChange={(e) => setPhone(e.target.value)} />              <select className="crm-input" value={clientId} onChange={(e) => setClientId(e.target.value)} title="Клиент (необязательно)">                <option value="">Клиент (необязательно)</option>                {(clientsData?.clients || []).map((c) => (                  <option key={c.id} value={c.id}>{c.name}{c.phone ? ` · ${c.phone}` : ""}</option>                ))}              </select>              <textarea                className="crm-input"                placeholder="Примечание (запчасти, цена, детали...)"                value={notes}                onChange={(e) => setNotes(e.target.value)}                rows={2}                style={{ gridColumn: "1 / -1", resize: "vertical", minHeight: 48 }}              />            </div>            <button              type="button"              className="crm-btn crm-btn-sm"              style={{ marginTop: 10 }}              disabled={!canSubmit || createMutation.isPending}              onClick={() => createMutation.mutate()}            >              {createMutation.isPending ? "..." : "Добавить"}            </button>          </div>            <div style={{ overflow: "auto", maxHeight: "calc(100vh - 280px)" }}>            <table className="data-table">              <thead>                <tr>                  <th>Дата</th>                  <th>Госномер</th>                  <th>Модель</th>                  <th>Работы</th>                  <th>Пробег</th>                  <th>Телефон</th>                  <th>Примечание</th>                  <th>Статус</th>                  <th></th>                </tr>              </thead>              <tbody>                {isLoading ? (                  <tr><td colSpan={9} style={{ textAlign: "center", color: "var(--text-muted)" }}>Загрузка...</td></tr>                ) : appointments.length === 0 ? (                  <tr><td colSpan={9} style={{ textAlign: "center", color: "var(--text-muted)" }}>Записей пока нет</td></tr>                ) : appointments.map((a: any) => (                  <tr key={a.id} style={a.status === "cancelled" ? { opacity: 0.5 } : undefined}>                    <td style={{ whiteSpace: "nowrap", fontWeight: 600 }}>{formatDateTime(a.scheduledAt)}</td>                    <td style={{ fontFamily: "monospace", fontWeight: 600, textTransform: "uppercase" }}>                      {a.vehicle?.plate || "—"}                    </td>                    <td>{carModel(a)}</td>                    <td style={{ maxWidth: 200 }}>{a.title}</td>                    <td style={{ color: "var(--text-muted)" }}>                      {a.vehicle?.mileage ?? a.mileage ?? "—"}                    </td>                    <td style={{ whiteSpace: "nowrap" }}>{a.clientPhone || "—"}</td>                    <td style={{ maxWidth: 240, fontSize: 12, color: "var(--text-muted)" }}>                      {a.notes || "—"}                      {a.clientName && (                        <span style={{ display: "block", fontSize: 11, marginTop: 2 }}>👤 {a.clientName}</span>                      )}                    </td>                    <td>                      <span style={{                        fontSize: 11,                        padding: "2px 8px",                        borderRadius: 6,                        background: a.status === "done" ? "var(--success-soft)" : a.status === "cancelled" ? "var(--danger-soft)" : "var(--accent-soft)",                      }}>                        {STATUS_LABELS[a.status] || a.status}                      </span>                    </td>                    <td>                      <div style={{ display: "flex", gap: 4, flexWrap: "nowrap" }}>                        {a.status === "scheduled" && (                          <button type="button" className="crm-btn crm-btn-ghost crm-btn-sm" onClick={() => updateMutation.mutate({ id: a.id, status: "confirmed" })} title="Подтвердить">✓</button>                        )}                        {["scheduled", "confirmed"].includes(a.status) && (                          <button type="button" className="crm-btn crm-btn-ghost crm-btn-sm" onClick={() => updateMutation.mutate({ id: a.id, status: "in_progress" })} title="В ремонт">🔧</button>                        )}                        {a.status === "in_progress" && (                          <button type="button" className="crm-btn crm-btn-ghost crm-btn-sm" onClick={() => updateMutation.mutate({ id: a.id, status: "done" })} title="Готово">✅</button>                        )}                        {a.status !== "cancelled" && a.status !== "done" && (                          <button type="button" className="crm-btn crm-btn-ghost crm-btn-sm" style={{ color: "var(--danger)" }} onClick={() => cancelMutation.mutate(a.id)} title="Отменить">✕</button>                        )}                      </div>                    </td>                  </tr>                ))}              </tbody>            </table>          </div>        </div>      </AppShell>    );  } 