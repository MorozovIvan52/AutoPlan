import { useState, useMemo } from "react"; import { useQuery } from "@tanstack/react-query"; import { apiFetch } from "../lib/fetch-api"; import { AppShell } from "../components/AppShell"; import { AppointmentModal } from "../components/AppointmentModal"; import { getWeekStart, addDays, formatWeekLabel, dayHeader, toDateInput } from "../lib/calendar"; import { parseDueAt, defaultReminderTime } from "../lib/datetime";  const STATUS_LABELS: Record<string, string> = {   scheduled: "╨Ч╨░╨┐╨╕╤Б╨░╨╜",   confirmed: "╨Я╨╛╨┤╤В╨▓╨╡╤А╨╢╨┤╤С╨╜",   in_progress: "╨Т ╤А╨╡╨╝╨╛╨╜╤В╨╡",   done: "╨У╨╛╤В╨╛╨▓", };  const STATUS_COLOR: Record<string, string> = {   scheduled: "var(--accent)",   confirmed: "var(--success)",   in_progress: "var(--warning)",   done: "var(--text-muted)", };  type SlotPick = { date: string; time: string } | null;  function appointmentCar(a: any): string {   const plate = a.plate || a.vehicle?.plate;   const make = a.make || a.vehicle?.make;   const model = a.model || a.vehicle?.model;   const car = [plate, [make, model].filter(Boolean).join(" ")].filter(Boolean).join(" ┬╖ ");   if (car) return car;   if (a.clientName) return a.clientName;   if (a.clientPhone || a.phone) return a.clientPhone || a.phone;   return "╨Р╨▓╤В╨╛ ╨╜╨╡ ╤Г╨║╨░╨╖╨░╨╜╨╛"; }  export default function CalendarPage() {   const [weekStart, setWeekStart] = useState(() => getWeekStart(new Date()));   const [slotPick, setSlotPick] = useState<SlotPick>(null);   const [editAppt, setEditAppt] = useState<any>(null);    const from = weekStart.toISOString();   const to = addDays(weekStart, 7).toISOString();    const { data, isLoading } = useQuery({     queryKey: ["calendar-appointments", from],     queryFn: () => apiFetch<{ appointments: any[] }>(`/api/service/appointments?from=${encodeURIComponent(from)}&to=${encodeURIComponent(to)}`),   });    const appointments = (data?.appointments || []).filter((a) => a.status !== "cancelled");    const byDay = useMemo(() => {     const weekDates = Array.from({ length: 7 }, (_, i) => toDateInput(addDays(weekStart, i)));     const groups: any[][] = Array.from({ length: 7 }, () => []);      for (const a of appointments) {       const dateStr = toDateInput(new Date(a.scheduledAt));       const dayIndex = weekDates.indexOf(dateStr);       if (dayIndex >= 0) groups[dayIndex].push(a);     }      for (const list of groups) {       list.sort((a, b) => new Date(a.scheduledAt).getTime() - new Date(b.scheduledAt).getTime());     }     return groups;   }, [appointments, weekStart]);    const openNew = (dayIndex: number) => {     const d = addDays(weekStart, dayIndex);     const pad = (n: number) => String(n).padStart(2, "0");     setSlotPick({       date: `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`,       time: defaultReminderTime(),     });     setEditAppt(null);   };    const closeModal = () => {     setSlotPick(null);     setEditAppt(null);   };    return (     <AppShell hideTopBar>       <div className="page-header" style={{ display: "flex", alignItems: "center", justifyContent: "space-between", flexWrap: "wrap", gap: 10 }}>         <h1 className="page-title">╨Ъ╨░╨╗╨╡╨╜╨┤╨░╤А╤М ╨╖╨░╨┐╨╕╤Б╨╡╨╣</h1>         <div style={{ display: "flex", alignItems: "center", gap: 8 }}>           <button type="button" className="crm-btn crm-btn-ghost crm-btn-sm" onClick={() => setWeekStart(addDays(weekStart, -7))}>тЖР</button>           <span style={{ fontSize: 13, fontWeight: 600, minWidth: 160, textAlign: "center" }}>{formatWeekLabel(weekStart)}</span>           <button type="button" className="crm-btn crm-btn-ghost crm-btn-sm" onClick={() => setWeekStart(addDays(weekStart, 7))}>тЖТ</button>           <button type="button" className="crm-btn crm-btn-ghost crm-btn-sm" onClick={() => setWeekStart(getWeekStart(new Date()))}>╨б╨╡╨│╨╛╨┤╨╜╤П</button>         </div>       </div>        <div className="page-body">         {isLoading ? (           <p style={{ color: "var(--text-muted)" }}>╨Ч╨░╨│╤А╤Г╨╖╨║╨░...</p>         ) : (           <div className="repair-cal">             {Array.from({ length: 7 }, (_, dayIndex) => {               const dayAppts = byDay[dayIndex];               return (                 <div key={dayIndex} className="repair-cal__day">                   <div className="repair-cal__day-head">                     <span>{dayHeader(weekStart, dayIndex)}</span>                     <button type="button" className="crm-btn crm-btn-ghost crm-btn-sm" onClick={() => openNew(dayIndex)}>                       + ╨Ч╨░╨┐╨╕╤Б╤М                     </button>                   </div>                   <div className="repair-cal__list">                     {dayAppts.length === 0 ? (                       <p className="repair-cal__empty">╨Э╨╡╤В ╨╖╨░╨┐╨╕╤Б╨╡╨╣</p>                     ) : (                       dayAppts.map((a) => {                         const { time } = parseDueAt(a.scheduledAt);                         const car = appointmentCar(a);                         const work = a.title || "╨а╨░╨▒╨╛╤В╤Л ╨╜╨╡ ╤Г╨║╨░╨╖╨░╨╜╤Л";                         return (                           <button                             key={a.id}                             type="button"                             className="repair-cal__card"                             style={{ borderLeftColor: STATUS_COLOR[a.status] || "var(--accent)" }}                             onClick={() => { setEditAppt(a); setSlotPick(null); }}                           >                             <div className="repair-cal__card-top">                               <span className="repair-cal__time">{time}</span>                               <span className="repair-cal__status">{STATUS_LABELS[a.status] || a.status}</span>                             </div>                             <p className="repair-cal__car">ЁЯЪЧ {car}</p>                             <p className="repair-cal__work">ЁЯФз {work}</p>                             {a.clientName && car !== a.clientName && (                               <p className="repair-cal__client">{a.clientName}</p>                             )}                           </button>                         );                       })                     )}                   </div>                 </div>               );             })}           </div>         )}         <p style={{ fontSize: 11, color: "var(--text-muted)", marginTop: 10 }}>           ╨Я╨╛╨║╨░╨╖╨░╨╜╤Л ╤В╨╛╨╗╤М╨║╨╛ ╨╖╨░╨┐╨╕╤Б╨╕ ╤Б ╨▓╤Л╨▒╤А╨░╨╜╨╜╤Л╨╝ ╨▓╤А╨╡╨╝╨╡╨╜╨╡╨╝ ┬╖ ┬л+ ╨Ч╨░╨┐╨╕╤Б╤М┬╗ тАФ ╨╜╨╛╨▓╨░╤П ┬╖ ╨Ъ╨╗╨╕╨║ ╨┐╨╛ ╨║╨░╤А╤В╨╛╤З╨║╨╡ тАФ ╤А╨╡╨┤╨░╨║╤В╨╕╤А╨╛╨▓╨░╨╜╨╕╨╡         </p>       </div>        {(slotPick || editAppt) && (         <AppointmentModal           initialDate={slotPick?.date}           initialTime={slotPick?.time}           appointment={editAppt}           onClose={closeModal}         />       )}     </AppShell>   ); }